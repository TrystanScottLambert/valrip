# generated by datamodel-codegen:
#   filename:  waves_maml_schema_v1p0.json
#   timestamp: 2025-12-04T04:42:54+00:00
#   1. Run: datamodel-codegen --input rip_validator/schemas/waves_maml_schema_v1p0.json --input-file-type jsonschema --output rip_validator/model_waves_maml.py.
#   2. Rename the classes to end with "Entry", otherwise there are conflicts with the "Field" class due to that being a reserved word.
#   3. Replace 'const' with 'Literal'.
#   4. Replace 'regex' with 'pattern'.
#   5. Replace Survey and License string types with the relevant enums from .data_types
#   6. Remove the pydantic.Extra import, import pydantic.ConfigDict, and replace the
#      "class Config: extra = Extra.forbid" with "model_config = ConfigDict(extra="forbid")""
#   7. Implement the customised field_validators (these can be found in the Git history if you need to regenerate this file)

from __future__ import annotations

from datetime import date as date_aliased
from typing import List, Optional, Union
from pydantic import (
    BaseModel,
    ConfigDict,
    Field,
    field_validator,
    StrictInt,
)
from pydantic_core import PydanticCustomError


from email_validator import EmailNotValidError, validate_email
from rip_validator.helper_validator_methods import (
    format_error_and_location,
    raise_waves_list_error,
    raise_waves_missing_error,
)
from rip_validator.ucd_validator import validate_ucd as astropy_validate_udc
from .WAVES_config import (
    MAML_VERSION,
    SurveyName,
    License,
    ANSI,
    WAVESCustomExceptions,
    WAVESDataTypes,
    EMAIL_REGEX,
)


class DOIEntry(BaseModel):
    model_config = ConfigDict(extra="forbid")
    DOI: str = Field(..., description="Valid DOI")
    type: str = Field(..., description="Type of DOI")


class DependEntry(BaseModel):
    model_config = ConfigDict(extra="forbid")
    survey: str = Field(..., description="The name of the dependent survey.")
    dataset: str = Field(..., description="The name of the dependent dataset.")
    table: str = Field(..., description="The name of the dependent table.")
    version: Union[str, float] = Field(
        ..., description="The version of the dependent table."
    )


class QCEntry(BaseModel):
    model_config = ConfigDict(extra="forbid")
    min: float = Field(..., description="Required data minimum value")
    max: float = Field(..., description="Required data maximum value")


class FieldEntry(BaseModel):
    model_config = ConfigDict(extra="forbid")
    name: str = Field(..., description="Required field name")
    unit: str = Field(..., description="Required unit of measurement")
    info: str = Field(..., description="Required short description")
    ucd: str = Field(..., description="Required string of semicolon-separated UCDs")
    data_type: str = Field(
        ..., description="Required data type (e.g., int32, string, bool, float64)"
    )
    qc: Optional[QCEntry] = Field(
        None, description="Optional quality control parameters"
    )


class WavesMamlSchema(BaseModel):
    model_config = ConfigDict(extra="forbid")

    survey: SurveyName
    dataset: str
    table: str
    version: StrictInt
    date: date_aliased

    author: str = Field(..., description="Required lead author name and <email>")
    coauthors: Optional[List[str]] = Field(
        None, description="Optional list of coauthor names and <emails>"
    )

    DOIs: Optional[List[DOIEntry]] = None
    depends: Optional[List[DependEntry]] = None
    description: str
    comments: Optional[List[str]] = None
    license: Optional[License] = None
    keywords: Optional[List[str]] = None
    MAML_version: float
    fields: List[FieldEntry]

    # ---- validators ----
    # pydantic does not recognise empty strings as missing values,
    # so we implement custom validation for empty strings in all fields
    # as "missing" values

    @field_validator("survey", mode="before")
    @classmethod
    def validate_survey(cls, survey: str) -> str:
        """
        Validates for:
        1. survey is not empty.

        Does not validate that survey is one of the accepted values. This is built into
        the pydantic validation that occurs after the custom validation, by defining survey
        as an enum, SurveyName.

        Raises
        ------
        ValueError
            If survey is empty.
        """
        if not survey:
            raise_waves_missing_error()
        return survey

    @field_validator("dataset", mode="before")
    @classmethod
    def validate_dataset(cls, dataset: str) -> str:
        """
        Validates for:
        1. dataset is not empty.

        Raises
        ------
        ValueError
            If dataset is empty.
        """
        if not dataset:
            raise_waves_missing_error()
        return dataset

    @field_validator("table", mode="before")
    @classmethod
    def validate_table(cls, table: str) -> str:
        """
        Validates for:
        1. table is not empty.

        Raises
        ------
        ValueError
            If table is empty.
        """
        if not table:
            raise_waves_missing_error()
        return table

    @field_validator("version", mode="before")
    @classmethod
    def validate_version(cls, version: str) -> str:
        """
        Validates for:
        1. version is not empty.

        Raises
        ------
        ValueError
            If version is empty.
        """
        if not version:
            raise_waves_missing_error()
        return version

    @field_validator("date", mode="before")
    @classmethod
    def validate_date(cls, date: str) -> str:
        """
        Validates for:
        1. date is not empty.

        Does not validate that date is an appropriate date object. This is built into
        the pydantic validation that occurs after the custom validation, by defining date
        as a data object.

        Raises
        ------
        ValueError
            If date is empty.
        """
        if not date:
            raise_waves_missing_error()
        return date

    @field_validator("author", mode="before")
    @classmethod
    def validate_author(cls, author: str) -> str:
        """
        Validates for:
        1. author is not empty,
        3. author adheres to the required syntax,
        4. author passes a validate_email check from email_validator package.

        Raises
        ------
        ValueError
            If author is empty,
            If author has incorrect syntax,
            If author is an invalid email (using the email_validator package).
        """
        if not author:
            raise_waves_missing_error()

        if not isinstance(author, str):
            raise ValueError(f"Invalid author '{author}'.")

        match = EMAIL_REGEX.match(author)
        if not match:
            raise ValueError(
                f"Invalid author {author}. Must be in the format 'Full Name <email@example.com>'.",
            )

        email = match.group("email")
        try:
            validate_email(email, check_deliverability=True)
        # Generic exception to catch when there is no internet.
        except Exception:
            try:
                validate_email(
                    email, check_deliverability=False, dns_resolver=False
                )  # Without checking deliverability (requires internet).
            # Non-generic exception to catch actual invalid email addresses.
            except EmailNotValidError as e:
                raise ValueError(f"Invalid email address '{email}': {e}.")
            # Warn user that the deliverability was not checked due to internet access.
            print(
                f"{ANSI.YELLOW}WARNING: deliverability of email address {email} cannot be checked.{ANSI.RESET}"
            )

        return author

    @field_validator("coauthors", mode="before")
    @classmethod
    def validate_coauthors(cls, coauthors: list[str]):
        """
        Validates for:
        1. coauthors is a list,
        2. list is not empty,
        3. each coauthor is not empty,
        4. each coauthor adheres to the required syntax,
        5. each coauthor passes a validate_email check from email_validator package.

        Raises
        ------
        ValueError
            If coauthors is not a list,
            If coauthors is empty,
            If any element in coauthors is empty,
            If any element in coauthors has incorrect syntax,
            If any element has an invalid email (using the email_validator package).
        """
        if not coauthors:
            raise_waves_missing_error()

        if not isinstance(coauthors, list):
            raise ValueError("Invalid coauthor  - this must be a list")

        error_messages = []
        for ii, coauthor in enumerate(coauthors):
            if not coauthor:
                error_messages.append(
                    format_error_and_location(
                        "coauthors", None, ii, None, None, "Field required"
                    )
                )
            else:
                match = EMAIL_REGEX.match(coauthor)
                if not match:
                    error_messages.append(
                        format_error_and_location(
                            "coauthor",
                            coauthor,
                            ii,
                            None,
                            None,
                            "Must be in the format 'Full Name <email@example.com>",
                        )
                    )
                    continue
                email = match.group("email")
                try:
                    validate_email(email, check_deliverability=True)
                # Generic exception to catch when there is no internet.
                except Exception:
                    try:
                        validate_email(
                            email, check_deliverability=False, dns_resolver=False
                        )  # Without checking deliverability (requires internet).
                    # Non-generic exception to catch actual invalid email addresses.
                    except EmailNotValidError as e:
                        error_messages.append(
                            format_error_and_location(
                                "coauthor",
                                coauthor,
                                ii,
                                None,
                                None,
                                f"Invalid email address {email}: {e}.",
                            )
                        )
                    # Warn user that the deliverability was not checked due to internet access.
                    print(
                        f"{ANSI.YELLOW}WARNING: deliverability of email address {email} cannot be checked.{ANSI.RESET}"
                    )

        error_message = "".join(error_messages)
        if error_messages:
            raise PydanticCustomError(
                WAVESCustomExceptions.LIST_EXCEPTION,
                error_message,
            )
        return coauthors

    @field_validator("DOIs", mode="before")
    @classmethod
    def validate_DOIs(cls, dois: str) -> str:
        """
        Validates for:
        1. DOIs is a list of DOI objects,
        2. list is not empty,
        3. each DOI is not empty,
        4. each of the (required) submodel fields (DOI, type) are not empty.

        Raises
        ------
        ValueError
            If DOIs is not a list,
            If DOIs is empty,
            If any element is empty,
            If any element contains a submodel field which is empty.
        """
        if not dois:
            raise_waves_missing_error()

        if not isinstance(dois, list):
            raise ValueError(f"Invalid DOIs {dois} - this must be a list")

        error_messages = []

        for ii, doi in enumerate(dois):
            if not doi:
                error_messages.append(
                    format_error_and_location(
                        "DOIs", None, ii, None, None, "Field required"
                    )
                )
            elif not isinstance(doi, dict):
                error_messages.append(
                    format_error_and_location(
                        "DOIs",
                        doi,
                        ii,
                        None,
                        None,
                        "Invalid DOI - this must be a DOI object",
                    )
                )
                raise ValueError(f"Invalid DOIs '{dois}'.")
            elif "DOI" not in doi.keys() or not doi["DOI"]:
                error_messages.append(
                    format_error_and_location(
                        "DOIs", doi, ii, "DOI", None, "Field required"
                    )
                )
            elif "type" not in doi.keys() or not doi["type"]:
                error_messages.append(
                    format_error_and_location(
                        "DOIs", doi, ii, "type", None, "Field required"
                    )
                )

        error_message = "".join(error_messages)
        if error_messages:
            raise PydanticCustomError(
                WAVESCustomExceptions.LIST_EXCEPTION,
                error_message,
            )
        return dois

    @field_validator("depends", mode="before")
    @classmethod
    def validate_depends(cls, depends: str) -> str:
        """
        Validates for:
        1. depends is a list of depend objects,
        2. list is not empty,
        3. each depend field is not empty,
        4. each of the (required) submodel fields (survey, dataset, table, version) are not empty.

        Raises
        ------
        ValueError
            If depends is not a list,
            If depends is empty,
            If any element is empty,
            If any element contains a submodel field which is empty.
        """
        if not depends:
            raise_waves_missing_error()

        if not isinstance(depends, list):
            raise ValueError(f"Invalid depends {depends} - this must be a list")
        error_messages = []
        for ii, depend in enumerate(depends):
            if not depend:
                error_messages.append(
                    format_error_and_location(
                        "depends", None, ii, None, None, "Field required"
                    )
                )
            elif not isinstance(depend, dict):
                error_messages.append(
                    format_error_and_location(
                        "depends",
                        depend,
                        ii,
                        None,
                        None,
                        "Invalid depend - this must be a depend object",
                    )
                )
            elif "survey" not in depend.keys() or not depend["survey"]:
                error_messages.append(
                    format_error_and_location(
                        "depends",
                        depend,
                        ii,
                        "survey",
                        None,
                        "Field required",
                    )
                )
            elif "dataset" not in depend.keys() or not depend["dataset"]:
                error_messages.append(
                    format_error_and_location(
                        "depends",
                        depend,
                        ii,
                        "dataset",
                        None,
                        "Field required",
                    )
                )
            elif "table" not in depend.keys() or not depend["table"]:
                error_messages.append(
                    format_error_and_location(
                        "depends",
                        depend,
                        ii,
                        "table",
                        None,
                        "Field required",
                    )
                )
            elif "version" not in depend.keys() or not depend["version"]:
                error_messages.append(
                    format_error_and_location(
                        "depends",
                        depend,
                        ii,
                        "version",
                        None,
                        "Field required",
                    )
                )

        error_message = "".join(error_messages)
        if error_messages:
            raise PydanticCustomError(
                WAVESCustomExceptions.LIST_EXCEPTION,
                error_message,
            )
        return depends

    @field_validator("description", mode="before")
    @classmethod
    def validate_description(cls, description: str) -> str:
        """
        Validates for:
        1. description is not empty.

        Raises
        ------
        ValueError
            If description is empty.
        """
        if not description:
            raise_waves_missing_error()
        return description

    @field_validator("comments", mode="before")
    @classmethod
    def validate_comments(cls, comments: str) -> str:
        """
        Validates for:
        1. comments is a list of strings,
        2. list is not empty,
        3. each comment is not empty.

        Raises
        ------
        ValueError
            If comments is not a list,
            If comments is empty,
            If any element is empty.
        """
        if not comments:
            raise_waves_missing_error()

        if not isinstance(comments, list):
            raise ValueError(f"Invalid comments {comments} - this must be a list")
        error_messages = []

        for ii, comment in enumerate(comments):
            if not comment:
                error_messages.append(
                    format_error_and_location(
                        "comments", None, ii, None, None, "Field required"
                    )
                )

        error_message = "".join(error_messages)
        if error_messages:
            raise PydanticCustomError(
                WAVESCustomExceptions.LIST_EXCEPTION,
                error_message,
            )
        return comments

    @field_validator("license", mode="before")
    @classmethod
    def validate_license(cls, license: str) -> str:
        """
        Validates for:
        1. license is not empty.

        Does not validate that license is one of the accepted values. This is built into
        the pydantic validation that occurs after the custom validation, by defining license
        as an enum, License.

        Raises
        ------
        ValueError
            If license is empty.
        """
        if not license:
            raise_waves_missing_error()
        return license

    @field_validator("MAML_version", mode="before")
    @classmethod
    def validate_MAML_version(cls, MAML_version: float):
        """
        Validates for:
        1. MAML_version is not empty,
        2. MAML_version is correct.

        Raises
        ------
        ValueError
            If MAML_version is empty,
            If MAML_version is incorrect.
        """
        if not MAML_version:
            raise_waves_missing_error()
        if MAML_version != MAML_VERSION:
            raise ValueError(
                f"Invalid MAML_version, value should be: {MAML_VERSION}.",
            )
        return MAML_version

    @field_validator("fields", mode="before")
    @classmethod
    def validate_fields(cls, fields: list[dict]):
        """
        Validates for:
        1. fields is a list of field objects,
        2. list is not empty,
        3. each field element is not empty,
        4. each of the (required) submodel fields (name, unit, info, ucd, data_type) are not empty,
        5. various more specific validation on the submodel fields which require it (ucd, data_type, ucd):
           a) validates ucd with the same method that astropy implements,
           b) validates that the provided field data_type is one of the WAVES-accepted data types,
           c) validates qc for:
              i) if qc is present, both subfields (min and max) are also present,
              ii) the qc value type matches the data_type provided for this field.
        6. no fields with the same name exist (field duplication).

        Raises
        ------
        ValueError
            If fields is not a list,
            If fields is empty,
            If any element is empty,
            If any element contains a submodel field which is empty,
            If any element contains an invalid submodel field,
            If any element has a name duplicated elsewhere.
        """
        if not fields:
            raise_waves_missing_error()

        if not isinstance(fields, list):
            raise ValueError("Invalid fields  - this must be a list")

        error_messages = []

        unique_names = dict[str, list[int]]()
        duplicates = {}
        for i, field in enumerate(fields):
            if not field:
                error_messages.append(
                    format_error_and_location(
                        "field", None, i, None, None, "Field required"
                    )
                )
            # If this is not a valid field object, do not keep validating this list item, just check the next one
            elif not isinstance(field, dict):
                error_messages.append(
                    format_error_and_location(
                        "fields",
                        field,
                        i,
                        None,
                        None,
                        "Invalid field - this must be a field object",
                    )
                )
            # If this is not a valid field object, do not keep validating this list item, just check the next one.
            # If this is a valid field object, identify any missing (required) fields
            else:
                field_name = None
                if "name" not in field.keys() or not field["name"]:
                    error_messages.append(
                        format_error_and_location(
                            "field", None, i, "name", None, "Field required"
                        )
                    )
                else:
                    field_name = field["name"]
                    # If "name" is present, check for duplicates
                    if field["name"] in unique_names.keys():
                        duplicates[f"{i}"] = field["name"]
                        error_messages.append(
                            f"\n → Incorrect element: {field['name']} (element ({i}). Duplicate fields are not allowed, see also element {unique_names[field['name']]}.\n",
                        )
                    else:
                        if field["name"] not in unique_names.keys():
                            unique_names[field["name"]] = []
                        unique_names[field["name"]].append(i)

                if "unit" not in field.keys() or not field["unit"]:
                    error_messages.append(
                        format_error_and_location(
                            "field", field_name, i, "unit", None, "Field required"
                        )
                    )
                if "info" not in field.keys() or not field["info"]:
                    error_messages.append(
                        format_error_and_location(
                            "field", field_name, i, "info", None, "Field required"
                        )
                    )
                if "ucd" not in field.keys() or not field["ucd"]:
                    error_messages.append(
                        format_error_and_location(
                            "field", field_name, i, "ucd", None, "Field required"
                        )
                    )
                else:
                    try:
                        _validate_ucd(field, i)
                    except PydanticCustomError as e:
                        error_messages.append(e.message())
                if "data_type" not in field.keys() or not field["data_type"]:
                    error_messages.append(
                        format_error_and_location(
                            "field",
                            field_name,
                            i,
                            "data_type",
                            None,
                            "Field required",
                        )
                    )
                else:
                    try:
                        _validate_data_type(field, i)
                    except PydanticCustomError as e:
                        error_messages.append(e.message())

                try:
                    _validate_qc(field, i)
                except PydanticCustomError as e:
                    error_messages.append(e.message())

        error_message = "".join(error_messages)
        if error_messages:
            raise PydanticCustomError(
                WAVESCustomExceptions.LIST_EXCEPTION,
                error_message,
            )

        return fields


def _validate_ucd(field: dict, index: int):
    """
    Validates UCDs with the same method that astropy implements.

    :param field: The whole field submodel (as a dict).
    :param index: The index of the field.

    Raises
    ------
    ValueError
        If field["ucd"] is empty,
        If field["ucd"] is an invalid astropy ucd.
    """
    field_name = None
    if "name" in field.keys() and field["name"]:
        field_name = field["name"]
    # Catching empty ucd field is necessary, otherwise it will not catch other invalid UCDs afterwards.
    # We use the same error message that pydantic applies to wrongly-empty fields.
    if field["ucd"]:
        try:
            astropy_validate_udc(field["ucd"])
        except ValueError as e:
            raise_waves_list_error(
                format_error_and_location(
                    "field", field_name, index, "ucd", field["ucd"], e
                )
            )


def _validate_data_type(field: dict, index: int):
    """
    Validates that the provided field data_type is one of the WAVES-accepted data types.

    :param field: The whole field submodel (as a dict).
    :param index: The index of the field.

    Raises
    ------
    ValueError
        If field["data_type"] is empty,
        If field["data_type"] is not a valid WAVESDataType.
    """
    field_name = None
    if "name" in field.keys() and field["name"]:
        field_name = field["name"]
    if "data_type" in field.keys() and field["data_type"] not in WAVESDataTypes:
        raise_waves_list_error(
            format_error_and_location(
                "field",
                field_name,
                index,
                "data_type",
                field["data_type"],
                f"data_type must be one of: {[e.value for e in WAVESDataTypes]}",
            )
        )


def _validate_qc(field: dict, index: int):
    """
    Validates for:
    1. If qc is present, both subfields (min and max) are also present.
    2. The qc value type matches the data_type provided for this field.

    :param field: The whole field submodel (as a dict).
    :param index: The index of the field.

    Raises
    ------
    ValueError
        If field["qc"] is empty,
        If field["qc"] is missing either submodel field,
        If field["qc"] is not the same type as field["data_type"].
    """
    field_name = None
    if "name" in field.keys() and field["name"]:
        field_name = field["name"]
    if (
        "data_type" in field.keys()
        and field["data_type"]
        and field["data_type"] not in WAVESDataTypes.numeric()
        and "qc" in field.keys()
        and field["qc"]
    ):
        raise_waves_list_error(
            format_error_and_location(
                "field",
                field_name,
                index,
                "qc",
                field["qc"],
                f"Only numeric fields may have a qc element, this field is type {field['data_type']}",
            )
        )

    # TODO: currently this case is only testing for either missing "min" or missing "max".
    # This is not the expected behaviour, but the issue is actually a global issue, not just a field['qc'] one.
    # There is a ticket (#52) to address this.
    if (
        "qc" in field.keys()
        and field["qc"]
        and ("min" not in field["qc"].keys() or "max" not in field["qc"].keys())
    ):
        raise_waves_list_error(
            format_error_and_location(
                "field",
                field_name,
                index,
                "qc",
                field["qc"],
                "Both min and max values must appear if field>qc is present.",
            )
        )

    if (
        "data_type" in field.keys()
        and "qc" in field.keys()
        and field["data_type"]
        and field["qc"]
    ):
        if field["data_type"] in WAVESDataTypes.integer() and (
            isinstance(field["qc"]["min"], float)
            or isinstance(field["qc"]["max"], float)
        ):
            raise_waves_list_error(
                format_error_and_location(
                    "field",
                    field_name,
                    index,
                    "qc",
                    field["qc"],
                    f"Field is of integer type ({field['data_type']}) but field>qc value(s) are of float type - types must match.",
                )
            )

        if field["data_type"] in WAVESDataTypes.floating_point() and (
            isinstance(field["qc"]["min"], int) or isinstance(field["qc"]["max"], int)
        ):
            raise_waves_list_error(
                format_error_and_location(
                    "field",
                    field_name,
                    index,
                    "qc",
                    field["qc"],
                    f"Field is of float type ({field['data_type']}) but field>qc value(s) are of integer type - types must match.",
                )
            )
